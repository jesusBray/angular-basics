import { NgModule } from '@angular/core';
import { GenericLoginComponent } from './generic-login.component';

@NgModule({
  declarations: [GenericLoginComponent],
  imports: [
  ],
  exports: [GenericLoginComponent]
})
export class GenericLoginModule { }
